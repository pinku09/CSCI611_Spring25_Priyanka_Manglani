To use this project you would need -
Miniconda/Anaconda installed.
Jupyter Notebook or VSCode with the Jupyter extension.

Setup
Create and activate the conda environment: conda env create -n ml -f environment.yml conda activate ml
Open the notebooks and select the ml kernel.
Execution Run the code in VS code(preferrable)